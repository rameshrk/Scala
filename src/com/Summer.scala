package com;

	import ChecksumAccumulator.calculate
	  object Summer {
	    def main(args: Array[String]) {
	      for (arg <- args)
	        println(arg +": "+ calculate(arg))
	        
	        (2.0).unary_-
	       (2.0).unary_+
//	       *.p
	        7.toLong;
	      val s =""
	      s.indexOf('o')
	       s indexOf('o')
	        s indexOf('o',8)
	        (2.0).unary_-
	        
	        val sl = "Hello, world!"
	         sl.toLowerCase
	         sl.toLowerCase()
	         sl toLowerCase
	} }

